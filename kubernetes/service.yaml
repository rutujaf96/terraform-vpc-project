apiVersion: v1
kind: Service
metadata:
  name: springboot-service
  namespace: springboot
spec:
  type: NodePort

  selector:
    app: springboot-app

  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080
